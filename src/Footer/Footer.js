import React from 'react';

const footer = () => {
    return (
        <div className="Footer">
            <p>This was developed for practise purpose only, online at {Date.now().toLocaleString()}</p>
        </div>
    );
};

export default footer;
